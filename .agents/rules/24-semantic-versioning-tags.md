# Semantic Versioning Tags

**Category:** release  **Confidence:** low

**Rule:**
> Release tags follow bare semantic versions. When bumping a version, update every location that carries it (e.g., `conanfile.py`, `CMakeLists.txt`) in the same commit.
