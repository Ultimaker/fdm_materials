# XML Profile Schema Compliance

**Category:** fdm_materials  **Confidence:** high

**Rule:**
> All material profiles must comply with the standard XML `.fdm_material` schema. Run `./run_check_material_profiles.sh` to validate schema compliance before committing.
